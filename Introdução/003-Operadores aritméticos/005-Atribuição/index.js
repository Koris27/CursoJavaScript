let a = 3;
let b = 2;
//console.log(a);
//console.log(a + b);
//a += 1;
//a *= 2;
//a -= 1;
//a /= 3;
console.log(a);


/* Variáveis
var (não é tão utilizado). 
let (é como se fosse uma variável que trabalha por escopo -> propriedade que determina onde uma variável pode ser utilizada como um identificador em um programa.
const(constante que não se altera no meio do caminho).
Operadores de atribuição
=, +=, -=, *=, /=
*/
